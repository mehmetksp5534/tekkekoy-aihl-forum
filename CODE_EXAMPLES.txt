╔════════════════════════════════════════════════════════════════════════════╗
║      PROFİL ÖZELLEŞTİRME SİSTEMİ - KOD ÖRNEKLERİ VE AÇIKLAMALAR        ║
╚════════════════════════════════════════════════════════════════════════════╝

═══════════════════════════════════════════════════════════════════════════════
1. ÇERÇEVE KALINLIĞININ ARTTIRILMASI (3px → 6px)
═══════════════════════════════════════════════════════════════════════════════

📁 templates/dashboard.html (Çerçeveler Sekmesi - Line 399)

KODDA ARANAN VER:
    border: 3px solid #333;

DEĞİŞTİRİLEN VER:
    border: 6px solid #333;

NEDEN:
  • Visual farkı net şekilde ortaya koymak için
  • Çerçeve seçimi daha belirgin hale getirildi
  • Responsive tasarımda mobile'da da görülür


═══════════════════════════════════════════════════════════════════════════════
2. PENDING STATE MANTIGI (Hemen Kaydetme → Kaydet Butonuna Bağla)
═══════════════════════════════════════════════════════════════════════════════

📁 templates/dashboard.html - JavaScript (Line 462-470)

EKLENEN KOD:
┌─────────────────────────────────────────────────────────────────────────┐
│ const pendingCustomizations = {                                          │
│     frame_id: null,                      // Çerçeve seçimi tutacak      │
│     badge_id: null,                      // Rozet seçimi tutacak        │
│     bg_color_id: null,                   // Renk seçimi tutacak         │
│     frameData: null,                     // Çerçeve detayları            │
│     badgeData: null,                     // Rozet detayları              │
│     colorData: null                      // Renk detayları               │
│ };                                                                       │
└─────────────────────────────────────────────────────────────────────────┘

NE İŞE YARAR:
  • Seçimleri hemen API'ye göndermez
  • Preview'da gösterir (UI feedback)
  • "Kaydet"a basılana kadar bekler
  • Birden fazla seçim aynı anda kaydetmeyi sağlar


═══════════════════════════════════════════════════════════════════════════════
3. UPDATED selectFrame() FUNCTION
═══════════════════════════════════════════════════════════════════════════════

📁 templates/dashboard.html (Line 630-651)

ESKİ KOD (Hemen Kaydetme):
┌─────────────────────────────────────────────────────────────────────────┐
│ function selectFrame(frameId, frameData, element) {                     │
│     // ❌ HEMEN API'ye gönder                                          │
│     fetch('/api/profile/customize', {                                   │
│         method: 'POST',                                                 │
│         body: JSON.stringify({frame_id: frameId})                      │
│     })                                                                  │
│     .then(response => {                                                │
│         // ... preview güncelle                                        │
│     });                                                                 │
│ }                                                                       │
└─────────────────────────────────────────────────────────────────────────┘

YENİ KOD (Pending State):
┌─────────────────────────────────────────────────────────────────────────┐
│ function selectFrame(frameId, frameData, element) {                     │
│     // ✅ Pending state'e kaydet (HEMEN KAYDETME)                      │
│     pendingCustomizations.frame_id = frameId;                           │
│     pendingCustomizations.frameData = frameData;                        │
│                                                                          │
│     // Visual feedback - seçilmiş göster                               │
│     document.querySelectorAll('#frames-grid .item-card').forEach(card   │
│         card.classList.remove('selected');                             │
│     });                                                                 │
│     element.classList.add('selected');                                 │
│                                                                          │
│     // Preview'da güncelle (Sadece UI, database değil)                │
│     document.getElementById('preview-frame-name').textContent =         │
│         frameData.name;                                                │
│                                                                          │
│     const photoElement = document.getElementById('preview-photo');     │
│     if (photoElement && frameData.color) {                             │
│         photoElement.style.borderColor = frameData.color;              │
│     }                                                                  │
│                                                                          │
│     console.log('Frame pending:', frameData.name);                     │
│ }                                                                       │
└─────────────────────────────────────────────────────────────────────────┘

FARKLAR:
  ❌ KALDIRILAN: fetch('/api/profile/customize', ...)
  ✅ EKLENEN: pendingCustomizations.frame_id = frameId;
  ✅ EKLENEN: pendingCustomizations.frameData = frameData;


═══════════════════════════════════════════════════════════════════════════════
4. UPDATED selectBadge() FUNCTION
═══════════════════════════════════════════════════════════════════════════════

📁 templates/dashboard.html (Line 654-674)

ESKİ KOD:
    fetch('/api/profile/customize', {
        method: 'POST',
        body: JSON.stringify({badge_id: badgeId})
    })

YENİ KOD:
    pendingCustomizations.badge_id = badgeId;
    pendingCustomizations.badgeData = badgeData;
    
    // Preview'da badge göster
    const badgePreview = document.getElementById('badge-preview-area');
    if (badgePreview && badgeData.icon) {
        badgePreview.innerHTML = `<img src="${badgeData.icon}" ...>`;
    }


═══════════════════════════════════════════════════════════════════════════════
5. UPDATED selectBgColor() FUNCTION
═══════════════════════════════════════════════════════════════════════════════

📁 templates/dashboard.html (Line 677-695)

ESKİ KOD:
    fetch('/api/profile/customize', {
        method: 'POST',
        body: JSON.stringify({bg_color_id: colorId})
    })

YENİ KOD:
    pendingCustomizations.bg_color_id = colorId;
    pendingCustomizations.colorData = colorData;
    
    // Preview'da rengi göster (gradient veya solid)
    const bgStyle = colorData.gradient_code ? 
                    colorData.gradient_code : 
                    colorData.color_code;
    preview.style.background = bgStyle;


═══════════════════════════════════════════════════════════════════════════════
6. YENİ saveAllCustomizations() FUNCTION
═══════════════════════════════════════════════════════════════════════════════

📁 templates/dashboard.html (Line 697-740)

TAMAMEN YENİ FONKSİYON:
┌─────────────────────────────────────────────────────────────────────────┐
│ function saveAllCustomizations() {                                      │
│     // 1. Pending seçimleri hazırla                                    │
│     const customizations = {};                                          │
│                                                                          │
│     if (pendingCustomizations.frame_id) {                               │
│         customizations.frame_id = pendingCustomizations.frame_id;      │
│     }                                                                  │
│     if (pendingCustomizations.badge_id) {                               │
│         customizations.badge_id = pendingCustomizations.badge_id;      │
│     }                                                                  │
│     if (pendingCustomizations.bg_color_id) {                            │
│         customizations.bg_color_id = pendingCustomizations.bg_color_id;│
│     }                                                                  │
│                                                                          │
│     // 2. Eğer hiçbir seçim yoksa uyar                                │
│     if (Object.keys(customizations).length === 0) {                     │
│         alert('Lütfen en az bir özelleştirme seçin');                  │
│         return;                                                        │
│     }                                                                  │
│                                                                          │
│     // 3. Tüm seçimleri birden API'ye gönder                         │
│     fetch('/api/profile/customize', {                                   │
│         method: 'POST',                                                │
│         headers: {'Content-Type': 'application/json'},                │
│         body: JSON.stringify(customizations)                          │
│     })                                                                  │
│     .then(response => response.json())                                 │
│     .then(result => {                                                  │
│         if (result.success || result.message) {                        │
│             // 4. Başarılı → uyarı ve temizle                        │
│             alert('✅ Profil özelleştirmeleri kaydedildi!');          │
│                                                                          │
│             // Pending state'i temizle                                 │
│             pendingCustomizations.frame_id = null;                     │
│             pendingCustomizations.badge_id = null;                     │
│             pendingCustomizations.bg_color_id = null;                  │
│             pendingCustomizations.frameData = null;                    │
│             pendingCustomizations.badgeData = null;                    │
│             pendingCustomizations.colorData = null;                    │
│         } else {                                                       │
│             alert('❌ Kaydetme sırasında hata oluştu');               │
│         }                                                              │
│     })                                                                  │
│     .catch(error => {                                                  │
│         console.error('Save error:', error);                           │
│         alert('❌ Kaydetme sırasında hata oluştu');                   │
│     });                                                                 │
│ }                                                                       │
└─────────────────────────────────────────────────────────────────────────┘

ADIMLAR:
  1️⃣  Pending seçimleri topla
  2️⃣  Hiçbir seçim yoksa alert göster
  3️⃣  Tüm seçimleri JSON olarak API'ye gönder
  4️⃣  Başarılı ise: uyarı + pending state temizle
  5️⃣  Hata ise: error message göster


═══════════════════════════════════════════════════════════════════════════════
7. KAYDET BUTONU (Her Sekmesinde)
═══════════════════════════════════════════════════════════════════════════════

📁 templates/dashboard.html

ÇERÇEVELER SEKMESİ (Line 415-420):
┌─────────────────────────────────────────────────────────────────────────┐
│ <div style="margin-top: 20px; text-align: center;">                    │
│     <button id="save-customization-btn"                                │
│             onclick="saveAllCustomizations()"                          │
│             style="background: #4CAF50; color: white;                  │
│                    padding: 10px 30px; border: none;                   │
│                    border-radius: 4px; cursor: pointer;                │
│                    font-size: 16px; font-weight: bold;">               │
│         💾 Kaydet                                                      │
│     </button>                                                          │
│ </div>                                                                 │
└─────────────────────────────────────────────────────────────────────────┘

ROZETLER SEKMESİ (Line 453-458):
    Aynı kodu kullan (onclick="saveAllCustomizations()")

RENKLER SEKMESİ (Line 485-490):
    Aynı kodu kullan (onclick="saveAllCustomizations()")

SONUÇ:
  ✓ Her sekmesinde "Kaydet" butonu var
  ✓ Hepsi aynı saveAllCustomizations() fonksiyonunu çağırır
  ✓ Hangi sekmesinde olursa olsun, tüm pending seçimler kaydedilir


═══════════════════════════════════════════════════════════════════════════════
8. VARSAYILAN (DEFAULT) RENK SEÇENEĞI
═══════════════════════════════════════════════════════════════════════════════

📁 templates/dashboard.html - loadBgColors() fonksiyonu (Line 559-577)

EKLENEN KOD:
┌─────────────────────────────────────────────────────────────────────────┐
│ // Varsayılan (Default) seçeneğini ekle - en başa                      │
│ const defaultCard = document.createElement('div');                     │
│ defaultCard.className = 'item-card';                                   │
│ defaultCard.setAttribute('data-color-id', 'default');                  │
│                                                                          │
│ // Varsayılan gradient'in stili                                       │
│ defaultCard.style.background =                                         │
│     'linear-gradient(135deg, #6E81FF 0%, #DFF7F7 100%)';             │
│ defaultCard.style.border = '2px solid #ddd';                          │
│                                                                          │
│ // Varsayılan kartının HTML                                           │
│ defaultCard.innerHTML = `                                              │
│     <div class="item-name" style="color: white; font-weight: bold;">  │
│         Varsayılan                                                     │
│     </div>                                                             │
│     <div class="item-xp" style="color: white;">                       │
│         Default Gradient                                              │
│     </div>                                                             │
│ `;                                                                     │
│                                                                          │
│ // Tıklandığında                                                      │
│ defaultCard.onclick = () => selectBgColor('default', {                │
│     id: 'default',                                                    │
│     name: 'Varsayılan',                                               │
│     gradient_code: 'linear-gradient(135deg, #6E81FF 0%, '            │
│                    '#DFF7F7 100%)',                                   │
│     color_code: '#6E81FF'                                             │
│ }, defaultCard);                                                       │
│                                                                          │
│ grid.appendChild(defaultCard);  // Gridi en başa ekle                 │
│                                                                          │
│ // Sonrasında diğer renkler normalmen render ediliyor                 │
│ colors.forEach(color => {                                             │
│     // ... normal renk render işlemi                                  │
│ });                                                                     │
└─────────────────────────────────────────────────────────────────────────┘

NEDEN:
  • Varsayılan gradient korunmalı
  • Kullanıcı istersen resetleyebilmeli
  • Orijinal değeri hiç kaybetmemelidir


═══════════════════════════════════════════════════════════════════════════════
9. GRADIENT vs SOLID COLOR (API Response)
═══════════════════════════════════════════════════════════════════════════════

API Response Formatı (app.py'da tanımlanıyor):

JSON RESPONSE:
┌─────────────────────────────────────────────────────────────────────────┐
│ {                                                                        │
│     "id": 1,                                                            │
│     "name": "Açık Mavi",                                                │
│     "color_code": "#E3F2FD",                                            │
│     "gradient_code": "linear-gradient(135deg, #6E81FF 0%, #DFF7F7)"    │
│ }                                                                        │
└─────────────────────────────────────────────────────────────────────────┘

JavaScript'te Kullanımı:
┌─────────────────────────────────────────────────────────────────────────┐
│ // Gradient varsa kullan, yoksa solid color kullan (fallback)          │
│ const bgStyle = colorData.gradient_code ?                               │
│                 colorData.gradient_code :                               │
│                 colorData.color_code;                                  │
│                                                                          │
│ preview.style.background = bgStyle;                                    │
└─────────────────────────────────────────────────────────────────────────┘

AVANTAJLAR:
  ✓ Gradient prioritet alır
  ✓ Gradient yoksa solid color fallback
  ✓ CSS'de dynamic olarak uygulanır
  ✓ Varsayılan gradient hep korunur


═══════════════════════════════════════════════════════════════════════════════
10. İKİ PREVIEW FOTOĞRAFI (Çerçeveler + Rozetler Sekmesi)
═══════════════════════════════════════════════════════════════════════════════

ÇERÇEVELER SEKMESİ:
    ID: preview-photo (Line 399)
    Boyut: 120x120px
    Border: 6px solid (çerçeve rengi)
    Amaç: Çerçeve seçiminin etkisini göster

ROZETLER SEKMESİ:
    ID: badge-preview-photo (Line 426)
    Boyut: 80x80px
    Border: 6px solid (çerçeve rengi)
    Amaç: Rozet seçimi yapılırken çerçevenin de görülmesi

selectFrame() FONKSİYONU:
┌─────────────────────────────────────────────────────────────────────────┐
│ // Her iki preview-photo'yu da güncelle                                │
│ const photoElement = document.getElementById('preview-photo');         │
│ const badgePhotoElement = document.getElementById('badge-preview-photo');│
│                                                                          │
│ if (photoElement && frameData.color) {                                 │
│     photoElement.style.borderColor = frameData.color;                  │
│ }                                                                       │
│ if (badgePhotoElement && frameData.color) {                            │
│     badgePhotoElement.style.borderColor = frameData.color;             │
│ }                                                                       │
└─────────────────────────────────────────────────────────────────────────┘

SONUÇ:
  ✓ Çerçeve seçimi yapılırsa, her iki sekmesinde de güncellenir
  ✓ Kullanıcı hangi sekmede olursa olsun, çerçevenin etkisini görür


═══════════════════════════════════════════════════════════════════════════════
📋 ÖZET - NEDENDiR BÖYLE YAPILDI
═══════════════════════════════════════════════════════════════════════════════

1. ÇERÇEVE KALINLIĞI 6px (Daha belirgin):
   → Kullanıcı bordürü daha net görebilir

2. PENDING STATE (Hemen Kaydetme):
   → Yanlış seçimlerin veritabanını kirletmesini engeller
   → Kullanıcı seçimini değiştirebilir
   → Birden fazla seçimi aynı anda yapabilir

3. KAYDET BUTONU (Kontrollü kaydetme):
   → Kullanıcı ne yaptığını bilebilir
   → Veritabanı güncellemeleri net olur
   → "Gönder" hissiyatı vermez, "Kaydet" demiş

4. DEFAULT OPTION (Varsayılana dön):
   → Özgün gradient korunur
   → Yanlış seçim yapılırsa reset edebilir
   → Orijinal tasarım hiç kaybedilmez

5. GRADIENT DESTEĞI (Renk + Gradient):
   → Daha zengin tasarım seçenekleri
   → Fallback ile compat ible
   → Herhangi bir renk gösterilebilir

6. İKİ PREVIEW (Çerçeveler + Rozetler):
   → Çerçeve seçimi yapılırken her sekmesinde görülür
   → Rozet seçimi yapılırken çerçeve hala aktif
   → Kullanıcı seçimlerinin birleşimini görebilir


═══════════════════════════════════════════════════════════════════════════════
✅ SONUÇ
═══════════════════════════════════════════════════════════════════════════════

Sistem istenen tüm gereksinimleri karşılıyor:
  ✅ Çerçeve kalınlığı artırıldı
  ✅ Seçimler pending state'de tuttuluyor
  ✅ Kaydet butonuna bağlandı
  ✅ Varsayılan (Default) seçeneği eklendi
  ✅ Gradient desteği korundu
  ✅ Tüm değişiklikler mevcut sistemi bozmadı
  ✅ Test edildi ve başarılı sonuç aldı
